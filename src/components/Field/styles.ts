import styled from 'styled-components'

import { colors } from '../../styles'

export const Field = styled.p`
  color: ${colors.white};
  font-size: 16px;
  font-weight: 300;
  margin-bottom: 12px;
  text-align: start;

  span {
    font-weight: 700;
  }
`
